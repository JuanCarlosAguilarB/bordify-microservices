spring:
  application:
    name: "api-gateway"
  cloud:
    gateway:
      routes:
        - id: auth-server
          uri: lb://auth-server
          predicates:
            - Path=/auth/**

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}


